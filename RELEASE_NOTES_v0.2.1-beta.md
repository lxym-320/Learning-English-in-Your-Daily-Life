# Context Lens 0.2.1 Beta

这是一个针对 macOS Chrome 选词兼容性的修复版本。

## 修复

- 修复 Chrome 网页中选中英文后不显示翻译按钮的问题。
- 支持读取 Chrome 的 Accessibility Text Marker Range。
- 当焦点元素不直接暴露选区时，继续检查父级和有限范围的网页节点。
- 保留松开鼠标后的延迟重试，适配选区更新较慢的网页。

## 隐私

选词过程仍然不会复制、读取或修改系统剪贴板。只有用户点击“翻译”后，所选文字才会发送给 DeepSeek。

## 安装

请彻底退出旧版，替换“应用程序”中的 Context Lens。由于 Beta 使用临时签名，升级后可能需要在“系统设置 → 隐私与安全性 → 辅助功能”中删除旧项并重新添加新版。
